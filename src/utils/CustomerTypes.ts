export interface Customer {
    id: string
    name: string
    phone: string
    email: string
    address: string
    accountNumber: string
    vehicle: string
    originalDebt: number
    currentDebt: number
}
